# 교환율 재기획 + 관제 그래프 — 세션 인수인계

> **작성일: 2026-07-30** · 컨텍스트 용량 문제로 세션을 새로 시작해야 해서 작성. 새 세션에서 이 문서를 먼저 읽고 이어서 진행하세요.
> 커밋 히스토리(`git log`)와 `업데이트기록.md`에 상세 내역이 다 있으니, 여기는 **"지금 뭐가 어디까지 됐고, 다음에 뭘 해야 하는지"** 요약입니다.

---

## 1. 전체 목표

교환대시보드가 지금까지 교환 "건수"만 보여줬는데, **BigQuery 출고 데이터를 연동해서 "출고량 대비 교환율"**을 계산·시각화하는 재기획 작업. 여기서 더 나아가 **통계적 임계치(평균±2σ)로 이상 징후를 자동 탐지하는 "관제 그래프"**까지 만드는 중.

작업 디렉토리: `/Users/deepdive/Documents/강희/Github/교환대시보드/cx-exchange-dashboard`
GitHub: `khlee-lang/CX-` (main 브랜치, 배포: https://cx-bice.vercel.app)

---

## 2. 완료된 것 (커밋 순서대로)

### 2-1. BigQuery 출고량 연동 — 분석 대시보드에 교환율·불량률 도입 (커밋 `d6a29f8`)
- 종합/자사몰/외부몰/불량/상품별 상세 **5개 분석 페이지 전부**에 교환율·불량률 지표 추가
- 공통 컴포넌트 추출: `KpiCard`/`ChartCard`/`DataTable`/`RateBadge`/`MatchCoverageChip`/`MinShipmentFilter`
- 핵심 lib: `src/lib/rate.ts` (교환율 계산 단일 진실 — 상품명 정규화, 채널 매핑, 최소출고량 임계값)
- API: `api/_bigquery.js`, `api/shipments.js` (판토스 BQ 출고 데이터 조회, `국내-B2C` 스코프)
- **네이버페이 특수 처리**: 시트엔 `[외부몰] 교환`으로 기재되지만 실제론 자사몰(카페24) 비회원 주문 — `src/lib/rate.ts`의 `isJasaScopedChannel()`로 자사몰 쪽에 합산. **스마트스토어는 완전히 별개 채널**(네이버페이와 혼동 금지, 사용자가 명확히 확인해줌)

### 2-2. 이지어드민/한솔물류 출고 히스토리 연동 (커밋 `4f17278`)
- 판토스 전환(2026-03-02) **이전** 출고 데이터(2024-05-02~2026-02-27, 78만 행)를 강희님이 CSV로 제공 → 정제해서 **BigQuery 신규 테이블**로 업로드
- 테이블: `verish-pm.cx.daily_channel_sku_stockout_historical` (라이브 테이블 `daily_channel_sku_stockout`과 거의 동일 스키마, sku_code/sku_option 포함)
- `api/shipments.js`가 라이브+과거 테이블을 `UNION ALL`로 조회 (출고 데이터 커버리지 5개월→약 2년)
- 실제 공백: 2026-02-28~03-01 (이틀, 데이터 자체가 없음)

### 2-3. 옛날 교환접수 시트 통합 (미커밋 — 세션 마지막에 작업, **커밋 필요**)
- 강희님이 준 옛날 교환접수 구글시트 3개(시트1: 2024-05~2025-01, 시트3: 2025-01~2025-07, 시트2: 2025-01~2026-01)를 dedup 통합
- **개인정보(수령자/연락처/우편번호/주소) 제외**, 주문번호는 포함(사용자 확인)
- 테이블: `verish-pm.cx.exchange_historical` (75,956행, 2024-05-28~2026-01-06)
- **⚠️ 중요 버그와 수정**: 처음엔 "시트2(최신 정리본) 우선"을 "그 기간 전체를 시트2로 교체"로 잘못 구현해서, 시트2가 실제로 텅 빈 2025-01~06 구간에서 시트3의 진짜 데이터(월 2,000~5,000건)를 버리고 있었음 (자사몰 교환율이 5개월간 0%로 무너짐). → **날짜별로 "그날 행이 더 많은 쪽"을 승자로 정하는 방식**으로 수정(51,305행 → 75,956행). 이 로직은 `/tmp/claude-501/consolidate_exchange_rowlevel.py`에 있는데 **이 스크립트는 임시 위치라 세션 재시작하면 사라짐** — 재사용하려면 이 문서의 [부록](#부록-옛날-교환시트-통합-스크립트-요약)에 로직 요약을 참고해 재작성 필요.
- API: `api/exchange-history.js` (신규, `EXCHANGE_HISTORY_MIN_DATE`/`MAX_DATE` 상수)

### 2-4. 관제 그래프 구현 (미커밋 — **커밋 필요**)
- 종합 대시보드에 **"교환율 관제 그래프"** 섹션 추가 (기존 "출고량 대비 교환율 추이" 이중축 차트를 대체)
- `src/lib/controlChart.ts`: `buildRateSeries()`(일/주/월 버킷팅), `computeBand()`(평균±2σ)
- `src/components/ui/ControlChart.tsx`: 자사몰/외부몰 2선 + 밴드(반투명 영역+점선) + 밴드초과 지점 빨간 점 강조
- **기준선 계산 구간**: `BASELINE_START='2024-07-22'` ~ `BASELINE_END='2025-12-29'` (76주) — 이유는 아래 "중요 발견" 참고
- 실측 밴드값: **자사몰** 평균 2.05% / 상한 3.56% / 하한 0.55%, **외부몰** 평균 1.13% / 상한 1.93% / 하한 0.34%
- `src/hooks/useExchangeHistory.ts`, `src/api/exchangeHistory.ts` 신규

---

## 3. 데이터 탐색 중 발견한 것들 (중요, 잊으면 안 됨)

1. **"시차 문제" 실제 사례**: 2024-05-28(교환 히스토리 시작 직후) ~2024-07-15까지 약 9주는 교환율이 거의 0%인데, 이건 데이터 문제가 아니라 "교환 접수 관행이 막 시작돼서 아직 축적 안 된 것" — 기준선 계산에서 제외(`BASELINE_START` 이유).
2. **월별 계절성 확인**: 5~6월이 평균보다 살짝 높고(2.19~2.35%), **7월이 오히려 역대 최저**(1.46%)라 "여름철 폭증" 계절 패턴은 없음. 그래서 지금(2026-07) 자사몰 교환율이 높게 나온다면 계절 때문이 아니라 진짜 이례적 신호.
3. **2026년 6월 실제 사건**: 라이브 데이터에서 2026-06-09~06-19 열흘간 자사몰 교환 접수가 하루 1,000~2,177건까지 폭증(평소 50~500건). **강희님 확인: "프로모션으로 문제가 많아서 접수 처리가 밀렸다가 한꺼번에 몰아서 입력"** — 즉 접수일 = 실제 요청일이 아니라 CS 처리일. 이게 왜 드릴다운 패널(원인 파악)이 필요한지 보여주는 실사례.
4. **2026-05-20~24 출고량 이상**: 5/23(토) 출고 0건은 정상(주말), **5/24(일) 11개는 평일(5/20 수) 밀린 주문을 주말에 처리한 것**(정상), 5/20(수, 평일)이 25개인 건 원인 불명(사용자도 모름 — 참고만).
5. **2025-10-06 주**: 자사몰·외부몰 둘 다 거의 0건 — 원인 불명이지만 사용자가 "무시해도 될듯, 연휴인 것 같다"고 함.
6. **매칭률**: BigQuery 출고 데이터와 시트 상품명 매칭률 99.98% (미매칭 1건은 상품명 칸에 옵션값이 잘못 입력된 시트 오류).

---

## 4. 사용자가 확정한 결정사항 (다시 묻지 말 것)

- 교환율 정의: **동일 기간 단순 비교** (출고 후 시차 보정은 안 함, 다만 경계 주는 밴드 계산에서 트림)
- 디자인: 기존 슬레이트/인디고 톤 유지 + 공통 컴포넌트화
- 매칭 키: sku_code(옵션은 name+color+size)
- 옛날 교환시트 dedup 우선순위: **시트2(최신 정리본) 우선** → 실제 구현은 "날짜별 행 수 많은 쪽" (위 2-3 참고)
- 개인정보: **일단 제외하고 나머지 먼저 업로드, 암호화(Cloud KMS AEAD)는 나중에 완성되면 채워넣기로** — 주문번호는 개인정보 아니라서 포함 확정
- 관제 그래프 임계치: 평균±2σ, 트림 구간 확정(2024-07-22~2025-12-29)
- 종합 대시보드 UI 피드백(아직 미반영, 다음 세션에서 처리):
  1. 운영지표 5종 줄(리드타임/처리율/회수지연/배송비/반복교환) **삭제**
  2. 교환율 KPI 4종 + 건수 KPI 5종 **두 줄을 하나로 병합** (교환율 카드 안에 건수·비중 흡수)
  3. 기존 "전체 출고 및 교환 추이" 차트(자사몰/외부몰/불량 3선, 출고량 없음) — **이미 관제 그래프로 대체 완료**(2-4 참고), 근데 코드에 옛날 잔재(`AreaChart` 섹션)가 아직 남아있는지 확인 필요
  4. 빈 날짜 구간 이어붙이기 — 자사몰 페이지는 이미 수정됨, 종합 대시보드도 확인 필요

---

## 5. BigQuery 테이블 전체 목록 (`verish-pm` 프로젝트, `cx` 데이터셋)

| 테이블 | 기간 | 용도 |
|---|---|---|
| `daily_channel_sku_stockout` | 2026-03-02~현재 | 판토스 출고 (기존, 라이브) |
| `daily_channel_sku_stockout_historical` | 2024-05-02~2026-02-27 | 이지어드민/한솔물류 출고 (내가 업로드) |
| `exchange_historical` | 2024-05-28~2026-01-06 | 옛날 교환접수 통합본, 75,956행, 개인정보 제외 |

권한: 서비스 계정 `cx-part@verish-part.iam.gserviceaccount.com`이 `cx` 데이터셋 단위로 Data Viewer 권한 보유 → **새 테이블 만들어도 자동으로 읽기 가능** (재요청 불필요, 실측 확인됨). 강희님 개인 계정(`kh.lee@deep-dive.kr`)은 테이블 생성/삭제까지 가능(gcloud 로그인 상태 유지 중이면 `gcloud auth print-access-token`으로 바로 사용 가능).

---

## 6. 지금 당장 다음 세션에서 해야 할 일 (우선순위 순)

1. **커밋 & 푸시** — 2-3(교환시트 통합)과 2-4(관제 그래프) 작업이 아직 커밋 안 됨. `git status`로 확인 후 커밋할 것. 커밋 전 `npx tsc --noEmit -p tsconfig.app.json` 필수(주의: `-p` 없이 돌리면 아무 파일도 검사 안 하고 항상 통과하는 함정 있음 — `tsconfig.json`이 `files:[]`).
2. **관제 그래프 마무리 검증** — 브라우저 프리뷰 도구가 이 세션 후반에 불안정했음(`window.innerWidth=0`이 되는 환경 버그, `resize_window`로 임시 해결). 새 세션에서 다시 스크린샷으로 실제 눈으로 확인 권장.
3. **드릴다운 패널** — 관제 그래프에서 밴드 벗어난 지점 클릭 시 그 시기 급증 기여 상품 TOP5 + 사유 분포 보여주는 패널 (설계는 이미 논의됨, 구현 안 됨).
4. **이상 이벤트 로그** — 밴드 초과 이력 자동 기록 + 메모 입력(영속 저장 방법 미정 — 새 구글시트 탭? BigQuery 테이블?).
5. **위 4번 항목의 종합 대시보드 UI 피드백 4가지** 반영.
6. **암호화 인프라(Cloud KMS)** — 개인정보(수령자/연락처/우편번호/주소)를 나중에 `exchange_historical`에 채워 넣기로 한 것. 구현 안 됨.
7. **자사몰/외부몰 채널별 관제 그래프** — 지금은 종합 대시보드에만 있음. 자사몰/외부몰 개별 페이지에도 넣을지 검토.

---

## 부록: 옛날 교환시트 통합 스크립트 요약

새 세션에서 재현이 필요하면 아래 로직으로 재작성:

**입력**: 구글시트 3개 (권한: 강희님 gcloud 계정, Sheets API로 직접 fetch)
1. `1W2viELxo7VrnU2cML2KYH6XlSY-yP9tyYZ2ge_6U07o` — 워크시트 `자사몰 교환`, `외부몰교환` (2024-05-28~2025-01-02)
2. `1ZK9CThK80iwgCZFxkBjKusdhhUD64c7iVMmbb9WZxdE` — `[수기] 자사몰 교환`, `[수기] 외부몰 교환`, `[수기] 자사/외부몰 ★불량★ 교환` (2025-01-01~2025-07-02, "시트3")
3. `1sqNyy94LFBooAly80uSrE5fEwY-qhU-6-vwIhS_rwM8` — 위와 동일 워크시트명 (2025-01-07~2026-01-06, "시트2")

**공통 컬럼 인덱스** (모든 세대 동일): 출고일=1, 채널/유형=2, 택배비=3, 지불방법 or 불량사유=4, 접수일=5, 주문번호=6, 교환전옵션=7, 상품명=8, 교환출고옵션=9, 수량=10.

**핵심 로직**:
- 접수일에 `(광복)2025-08-12` 같은 공휴일 주석이 붙은 경우가 있음(시트2 자사몰 212건) → 정규식으로 벗겨냄. **이미 시트 원본도 수정해둠**(강희님 요청으로 실제 구글시트 셀도 정리했음, F8631:F8842 범위).
- 채널 분류: `네이버페이`는 자사몰로, 오프라인/스타필드/플래그/팝업/라네띠/철회/(CS)클레임은 제외, 나머지는 외부몰.
- **날짜별 승자 결정**: 그 날짜에 대해 시트1/2/3 중 **행수가 가장 많은 세대**를 승자로 선택(우선순위 2>3>1은 동점 처리용으로만). 이게 핵심 — range 기반이 아니라 밀도 기반이어야 함.
- 시트1(2024년)은 불량이 별도 시트 없이 `지불방법` 열에 섞여 있음 (정상값: 무료교환/입금확인/철회/결제/무상/입금요청/차감/확인중/동봉확인/동봉/교환불가, 그 외 값은 전부 불량 사유로 취급).
- 개인정보 컬럼(수령자/연락처/우편번호/주소)은 아예 안 뽑음.
- `\r`/`\n`이 셀 값에 섞여 CSV가 깨지는 문제 있었음 → 모든 문자열 필드에 `clean_str()`(개행 제거) 적용 필수.

**출력 스키마** (BigQuery `exchange_historical`):
```
receipt_date DATE, ship_date STRING, order_no STRING, product_name STRING,
option_before STRING, option_after STRING, qty INTEGER,
channel_group STRING(jasa|oebu), channel_raw STRING, exchange_type STRING,
reason_or_pay_method STRING, is_defect BOOLEAN, shipping_fee INTEGER,
generation INTEGER(1|2|3), source_sheet STRING
```
