# 교환 대시보드 업데이트 기록 (CHANGELOG)

> 이 파일은 Claude가 작업할 때마다 **변경한 내용을 위에서부터(최신순) 추가**합니다.
> 형식: `## YYYY-MM-DD` 아래에 변경 항목을 적습니다. 무엇을 / 왜 / 어디(파일) 를 남깁니다.

---

## 2026-07-23

### 리터니즈 워크시트 맨 왼쪽 "메모" 열 추가 대응
- **배경**: 담당자용 자유 메모 목적으로 리터니즈 워크시트 A열에 "메모" 헤더 열을 신규 추가. 기존 열이 전부 오른쪽으로 한 칸씩 밀림(`구분` C→D열 등). 리터니즈 관련 코드 영향도를 먼저 점검함.
- **점검 결과**: 리터니즈 파이프라인(`upload-returnize.js`/`recompute-returnize.js`/`recompute-category.js`/`reconcile.js`/`server/index.js`)의 열 참조는 전부 헤더 텍스트 기반(`resolveColumns`, `api/_columns.js`)이라 위치 밀림에 안전. 데이터 처리상 깨지는 코드 없음. 현재 18열이라 읽기 상한 `A2:Z`(26열)도 여유.
- **중간 이슈(해결됨)**: 추가 직후 맨 오른쪽 기존 "메모"(S열)와 헤더가 중복됐으나(코드가 "메모" 헤더를 참조하지 않아 동작엔 무해) 담당자가 S열을 삭제해 A열 "메모" 하나만 남김. 최종 18열 확인.
- **문구 정리**: 실제 위치와 어긋나던 "C열" 하드코딩 표기를 향후 열 밀림에도 안 깨지도록 열 문자를 제거하는 방향으로 수정.
  - `src/pages/ReturnizeUpload.tsx`: 제목/안내문 "구분값(C열) 재계산" → "구분값 재계산"(2곳), 상단 주석 1곳
  - `api/upload-returnize.js`: 조건부서식 주석 "C열 구분값/L열 훼손" → 열 문자 제거
  - `api/recompute-returnize.js`: 헤더 주석 "구분열(C열)" → "구분열"
- **검증**: 텍스트/주석 변경만이라 로직 영향 없음. 서비스계정으로 리터니즈 헤더 직접 조회해 최종 열 구성(18열, 메모 1개) 확인.

### 반품-교환 연동 "출고일 날짜 변경하기" 추가
- **배경**: 반품-교환 연동에서 자동계산되는 출고일은 3형태 — ①`YYYY-MM-DD`(지불방법 정상 + 출고옵션 전부 채워짐 → 다음 영업일) ②`입고MMDD`(입금요청이거나 출고옵션 일부 비어있음) ③`이미출고됨(...)`(선출고건). 이 중 ①만 담당자가 원하는 날짜로 바꿔 일괄 발송하고 싶은 니즈가 있었음. ②③과 확인완료일은 의미가 달라 건드리면 안 됨.
- **백엔드**(`api/reconcile.js`, `server/index.js`): `shipDateOverride`(`YYYY-MM-DD`) 파라미터 추가. `reconcile()`에서 "다음 영업일" 케이스(`excRows.every(r=>r.newOpt)` 분기)에만 override 날짜를 대신 쓰고 사유를 "출고일 수동지정(...)"으로 기록. 입고MMDD·이미출고됨 분기는 그대로. 핸들러 진입 시 `YMD_RE`로 형식 검증(불일치 시 400). **두 백엔드는 reconcile 로직이 각각 인라인 복사본이라 동일하게 수정**.
- **프론트엔드**(`src/components/ReconcileSection.tsx`): "시트에 반영" 옆에 "출고일 날짜 변경하기" 버튼 + 날짜 선택 패널 추가. 미리보기 결과에 `YYYY-MM-DD` 출고일 건이 하나라도 있을 때(`canChangeShipDate`)만 노출. 날짜 선택 시 미리보기 표의 해당 건 출고일·사유를 즉시 교체해 보여주고(`displayActions`), "시트에 반영"(`apply=true`)일 때만 `shipDateOverride`를 백엔드로 전송(미리보기 요청엔 항상 `null`). 새 미리보기·탭 전환 시 override 초기화.
- **검증**: `tsc --noEmit` 통과, `vite build` 성공. 현재 대기 건이 0이라 버튼 노출 end-to-end(미리보기→날짜선택→시트반영)는 다음 실제 반품 배치에서 확인 예정.
- **남은 일**: 커밋+배포(현재 로컬만 반영), 실배치 end-to-end 확인.
- **변경 파일**: `cx-exchange-dashboard/api/reconcile.js`, `cx-exchange-dashboard/server/index.js`, `cx-exchange-dashboard/src/components/ReconcileSection.tsx`

## 2026-07-22

### 리터니즈 옵션 변환 규칙 보강 + 그리드 그로우 자동화 + 반영 버튼 로딩바
- **배경**: 실제 업로드 파일(`주문목록_20260722.xlsx`, 1289행)을 분석해보니 옵션 파싱 실패가 41건 있었음. 대부분(38건)이 반품업체 표기가 파이프(`|`) 대신 슬래시(`/`)로 온 케이스(`"S21651 / 블랙 / L"`), 나머지는 옵션 칸이 아예 비어있거나(2건) 상품명에 `", Option:색상^사이즈"` 형태로 캐럿 구분자가 붙어 들어오는 케이스(1건, `"티셔츠 브라 라이트, Option:뮤트블루^85C"`).
- **`buildItemOption` (`lib/returnizeTransform.ts`) 보강**:
  - 슬래시(`/`) 3파트 구분자 지원 추가(파이프와 동일 규칙)
  - 옵션 칸이 비어있으면 `null`(확인필요) 대신 상품명만으로 진행하도록 변경(일반 규칙 — 특정 주문번호 한정 아님). `ReturnizeRow`에 `optionMissing` 플래그 추가해 프론트에서 구분 가능하게 함
  - `", Option:색상^사이즈"` 패턴 전용 처리 추가: 이 패턴일 때만 `PRODUCT_NAME_MAP`(하드코딩 매핑 테이블, 현재 `'티셔츠 브라 라이트' → '티셔츠 브라 라이트 2.0'` 1건)으로 상품명 버전을 교정하고 캐럿을 대시로 변환(`뮤트블루^85C` → `[뮤트블루-85C]`). 나중에 비슷한 버전 교정 케이스 생기면 이 표에 한 줄만 추가하면 됨(대시보드에서 직접 관리하는 UI는 추후 과제로 남김).
- **프론트엔드**(`ReturnizeUpload.tsx`): "자동 변환 완료" 미리보기에서 `optionMissing`인 행에 주황 배경 + `⚠️ 옵션없음` 배지(툴팁: "옵션값이 비어있습니다. 리터니즈한테 확인요청해주세요!") 표시 + 상단 요약 배너 추가.
- **그리드 크기 초과 오류 대응**(`api/upload-returnize.js`): 실제 업로드 중 `Range exceeds grid limits (Max rows: 1077)` 오류 발생 — 리터니즈 시트에 이관할 행이 그리드 물리적 행 수를 넘어서 `values.update`가 거부됨. `ensureGridCapacity` 추가: 쓰기 전 시트 메타데이터로 그리드 행 수를 확인하고, 필요하면 +500행 여유를 두고 `batchUpdate`로 그리드를 늘리면서, 그리드 끝까지 걸려있던 조건부서식(C열 구분값 색칠 3종, L열 "훼손" 색칠)도 새 그리드 끝까지 같이 늘려서 새 행에도 서식이 유지되게 함.
- **반영 버튼 로딩바**: "구분값(C열) 재계산"의 "실제 반영" 버튼과 `ReconcileSection`(반품-교환 연동)의 "시트에 반영" 버튼이 `applying` 상태에서도 클릭 가능하고 스피너/로딩바가 없었음 — 업로드 반영 버튼과 동일한 패턴(스피너+"반영 중..." 텍스트+진행바, 버튼 비활성화)을 추가.
- **검증**: 실제 업로드 파일 데이터로 슬래시/빈옵션/캐럿 케이스 파싱 결과 확인(41건 실패 → 3건으로 감소, 남은 3건은 원본 데이터 자체 이슈). TypeScript 컴파일 통과. 브라우저에서 두 로딩바 모두 실제 클릭으로 렌더링 확인(반영 fetch는 가로채서 실제 시트 쓰기 없이 검증). `ensureGridCapacity`는 실제 그리드를 늘리는 부분이라 운영 시트에 쓰기가 발생해 테스트 목적으로 실행하지 않았고, 읽기 전용 메타데이터 조회로 현재 그리드(4075행)와 조건부서식 범위만 확인.
- **변경 파일**: `cx-exchange-dashboard/src/lib/returnizeTransform.ts`, `cx-exchange-dashboard/src/pages/ReturnizeUpload.tsx`, `cx-exchange-dashboard/api/upload-returnize.js`, `cx-exchange-dashboard/src/components/ReconcileSection.tsx`

## 2026-07-15 (3)

### 반품-교환 연동을 리터니즈 시트에도 지원 (판토스 로직 재사용) + UI 공용 컴포넌트화
- **배경**: 지금까지 반품-교환 연동(반품시트↔교환접수시트 대조해 출고일·확인완료일 자동 기입)은 판토스_입고리스트에만 있었음. 리터니즈도 동일한 처리가 필요한데, 로직은 완전히 같고 "읽고 쓰는 반품시트"만 다름(교환접수시트 `[자사몰]`/`[외부몰] 교환`은 두 채널 공통).
- **백엔드**(`api/reconcile.js`, `server/index.js`): `source` 파라미터 추가(`판토스`/`리터니즈`, 기본값 판토스로 하위호환). `RETURNS_SOURCES` 설정으로 반품시트별 차이를 흡수 — 판토스는 3행부터·`수량`+`실수량`, 리터니즈는 2행부터·`실수량`만(수량 열 없음, parseReturns가 실수량 우선이라 문제없음). 교환접수시트 시작행은 기존 판토스 동작 보존 위해 3으로 고정(2행의 오래된 데이터 1건은 최신 반품과 매칭될 일 없어 무해). 완료일은 각 반품시트에, 출고일은 공통 교환접수시트에 기입.
- **프론트엔드**: 판토스 페이지의 `ReconcileSection`을 `src/components/ReconcileSection.tsx` 공용 컴포넌트로 추출(`source` prop). 판토스는 `source="판토스"`, 리터니즈 업로드 페이지에 `source="리터니즈"` 섹션 추가. 자사몰/외부몰 탭·미리보기→시트반영 2단계 동작 동일.
- **검증**: 판토스 자사몰/외부몰(회귀), 리터니즈 자사몰(이슈 4건 카테고리혼재 감지)/외부몰, 잘못된 source 400 방어를 API로 확인. 브라우저에서 리터니즈 반품-교환 연동 미리보기까지 실행해 이슈 표 렌더링·콘솔 에러 없음 확인(실제 시트 반영은 미실행).
- **변경 파일**: `cx-exchange-dashboard/api/reconcile.js`, `cx-exchange-dashboard/server/index.js`, `cx-exchange-dashboard/src/components/ReconcileSection.tsx`(신규), `cx-exchange-dashboard/src/pages/PantosOps.tsx`(로컬 ReconcileSection 제거→공용 사용), `cx-exchange-dashboard/src/pages/ReturnizeUpload.tsx`

## 2026-07-15 (2)

### 교환접수시트 "[자동]" 식별자 배열수식 붕괴 발견 + 반품-교환 연동 "이미완료" 순서 버그 + 리터니즈 구분값 재계산 버튼 추가
- **배열수식 붕괴 발견**: 사용자가 특정 리터니즈 주문(`20260709-0014471`)이 "자사몰교환"이어야 하는데 "반품"으로 잘못 분류된 걸 제보. 원인 추적 결과 `[자사몰] 교환`·`[불량] 교환` 시트의 "[자동]" 식별자 열(주문번호+상품명+교환전옵션을 합쳐주는 `ARRAYFORMULA`)이 누군가 그 열 뒷부분에 값을 직접 입력해 배열 확산 범위와 충돌 → `#REF!` 에러로 전체가 깨져 있었음. 그 결과 `[자사몰] 교환`은 17,142행 중 16,941행(99%), `[불량] 교환`은 1,872행 중 1,866행(99.7%)의 식별자가 빈 값이었고, `recompute-category.js`의 매칭 맵이 이 두 시트 데이터를 거의 못 읽어서 실제로는 정상 교환 신청 건들이 조용히 "반품"으로 잘못 분류되고 있었음(`[외부몰] 교환`은 정상). 사용자가 시트 수식을 직접 복구한 뒤, 코드 쪽 변경 없이 `buildCategoryMap`/`recomputeForSheet`를 재실행해 리터니즈 시트(243행)를 다시 계산 — 오분류 건들이 정확히 재분류되는 것을 확인.
- **반품-교환 연동 "이미완료" 순서 버그**: 위 사고 여파로 일부 주문(`20260703-0010771`, `20260703-0011109`)이 카테고리혼재 확인필요로 남아있었는데, 시트 수식 복구 후 재계산되며 실제로는 전체 항목이 정상 재분류·처리 완료된 상태가 됨. 그런데 `reconcile.js`가 "카테고리 섞임" 체크를 "이 주문 전체가 이미 다 처리 완료됐는지" 체크보다 먼저 하고 있어서, 완전히 끝난 주문(`20260706-0008798` 등)도 계속 "확인필요"로 남아 화면에 뜨는 문제를 발견. `allRetRows.every(r => r.done)`이면 카테고리 혼재 체크보다 먼저 건너뛰도록 순서를 바꿔서 수정(`api/reconcile.js`, `server/index.js`).
- **리터니즈 구분값(C열) 재계산 버튼 추가**: 지금까지 리터니즈 구분값은 업로드 시점에만 자동 재계산됐는데(위 사고처럼 교환접수시트 쪽 데이터가 나중에 바뀌는 경우 등), 업로드 없이 독립적으로 다시 계산할 방법이 없었음. `api/recompute-returnize.js` 신규(판토스용 `recompute-category.js`의 `buildCategoryMap`/`recomputeForSheet`를 그대로 재사용, 리터니즈 시트 전용) + `ReturnizeUpload.tsx`에 판토스 페이지와 동일한 패턴의 "구분값(C열) 재계산" 섹션(미리보기→실제 반영) 추가. `server/index.js`에도 로컬 개발용 라우트 추가.
- **변경 파일**: `cx-exchange-dashboard/api/reconcile.js`, `cx-exchange-dashboard/api/recompute-returnize.js`(신규), `cx-exchange-dashboard/server/index.js`, `cx-exchange-dashboard/src/pages/ReturnizeUpload.tsx`

## 2026-07-15

### 판토스/리터니즈 자동화 페이지 완전 분리 + 리터니즈 반품 업로드 신규 + 반품-교환 연동 지연도착 사고 대응
- **페이지 구조 개편**: 기존 `반품-교환 연동`(`/reconcile`)과 `반품 시트 관리`(`/returns-sheet-maintenance`) 두 페이지를 없애고, **`판토스 반품 처리`(`/pantos`, `PantosOps.tsx`)** 하나로 합침 — 반품-교환 연동 + 구분값(E열) 재계산 + 완료건→히스토리 이관을 판토스_입고리스트 기준으로 한 곳에서 처리. 사이드바 "자동화" 섹션도 "자동화 · 판토스" / "자동화 · 리터니즈"로 분리.
- **리터니즈 반품 업로드 신규**(`/returnize-upload`, `ReturnizeUpload.tsx`): 리터니즈에서 다운로드한 "주문 조회 결과" 엑셀(.xlsx)을 업로드하면 `lib/returnizeTransform.ts`가 리터니즈 워크시트 양식(주문번호/성함/판매처/상품명옵션명/실수량 등)으로 변환. 검품 상태 "제품 없음" 행은 자동 제외, 옵션 형식이 "제품코드 | 색상 | 사이즈" 3파트가 아니면 "확인필요"로 분류해 사람이 상품명/옵션명을 직접 입력해야 반영 가능. 반영 시 `api/upload-returnize.js`가 시트에 행 추가 + 리터니즈 시트만 대상으로 구분값 자동 재계산까지 처리. 엑셀 파싱에 `xlsx` 패키지 신규 추가.
- **구분값 재계산 판토스 전용으로 되돌림**: 위 리터니즈 업로드 작업 도중, "구분값(E열) 재계산" 버튼(`api/recompute-category.js`)이 판토스_입고리스트+리터니즈 둘 다 한 번에 처리하도록 바뀌어 있었는데, 판토스 페이지를 판토스 전용으로 분리한 것과 안 맞아서 다시 판토스_입고리스트만 처리하도록 되돌림. 리터니즈는 리터니즈 업로드 화면에서 `recomputeForSheet`를 직접 호출해 자기 것만 계산하는 구조로 유지. `PantosOps.tsx`의 안내 문구("판토스_입고리스트 + 리터니즈 둘 다 대상")도 "(판토스_입고리스트 전용)"으로 수정.
- **반품-교환 연동 — "지연 도착" 카테고리혼재 사고 대응**: 사용자가 주문번호 `20260703-0010771`(반품 7개 중 자사몰교환 신청은 2개), `20260703-0011109`(반품 4개 중 신청 3개)가 "정상 처리"로 넘어간 게 이상하다고 제보. 원인은 `parseReturns`가 완료일이 채워진(이미 처리된) 행을 애초에 그룹에서 제외하고 있었던 것 — 신청한 상품만 먼저 판토스_입고리스트에 들어와 딱 맞게 매칭·처리된 뒤, 상관없는 반품 상품이 **나중에** 같은 주문번호로 들어오면, 그 시점엔 미처리 행만 봐서 카테고리혼재(2026-07-10 도입)를 감지할 대상 자체가 없어 조용히 지나감. 대조: 같은 시점에 제보된 `20260703-0010786`(반품 3개=신청 3개, 정상)은 문제 없음 확인.
  - **해결**: `parseReturns`가 완료 처리된 행도 `done` 플래그와 함께 그룹에 포함하도록 변경. `reconcile()`은 카테고리 혼재 여부를 (완료 여부 상관없이) 그룹 전체 기준으로 판단하고, 실제 매칭/처리 대상은 미완료 행만 사용. 이미 일부 처리된 뒤 카테고리가 섞인 경우 이슈 설명 문구도 구분("이미 일부 처리 완료됐는데 이후 구분값이 다른 반품 항목이 추가로 들어왔습니다").
  - **검증**: 수정 후 `20260703-0010771`/`20260703-0011109` 둘 다 "카테고리혼재" 이슈로 정확히 잡히고, `20260703-0010786`은 이슈 없이 통과되는 것을 실제 데이터로 확인. `api/reconcile.js`·`server/index.js`(로컬 개발서버, 별도 중복 구현) 양쪽 다 동일하게 수정.
- **변경 파일**: `cx-exchange-dashboard/api/reconcile.js`, `cx-exchange-dashboard/api/recompute-category.js`, `cx-exchange-dashboard/api/upload-returnize.js`(신규), `cx-exchange-dashboard/server/index.js`, `cx-exchange-dashboard/src/lib/returnizeTransform.ts`(신규), `cx-exchange-dashboard/src/pages/PantosOps.tsx`(신규, `Reconcile.tsx`+`ReturnsSheetMaintenance.tsx` 대체), `cx-exchange-dashboard/src/pages/ReturnizeUpload.tsx`(신규), `cx-exchange-dashboard/src/App.tsx`, `cx-exchange-dashboard/src/components/layout/Sidebar.tsx`, `cx-exchange-dashboard/package.json`(`xlsx` 추가)

## 2026-07-10

### 반품-교환 연동 — 카테고리혼재 감지 추가 (신청 안 한 상품이 같이 반품되는 케이스)
- **발견 경위**: 사용자가 주문번호 `20260630-0007933`이 "정상 처리"됐는데 실제로는 이상하다고 제보. 확인해보니 반품입고시트에 이 주문번호로 **브라 5개(구분=자사몰교환) + 팬티 5개(구분=반품)** 총 10행이 들어와 있었음. 팬티는 이 주문의 교환 신청 목록에 없어서(고객이 신청 안 한 상품을 반품 상자에 같이 넣어 보낸 것으로 추정) `구분값 재계산`이 "반품"으로 분류했고, `반품-교환 연동`은 카테고리로 먼저 걸러낸 뒤 개수를 비교하는 구조라 팬티 5개의 존재 자체를 몰랐음. 남은 브라 5개만 교환접수시트 5개와 맞아서 "정상 처리"로 조용히 통과됨. (이건 열 구조 변경 사고와는 무관한 별개의 로직 설계 갭 — 감사해보니 이 자동화가 버그 상태로 반영된 적은 없었음)
- **해결**: `parseReturns`에서 카테고리로 미리 걸러내지 않고 완료일 비어있는 행을 전부 주문번호별로 그룹핑(각 행에 구분값도 같이 저장). `reconcile()`에서 그룹 안에 구분값이 2종류 이상 섞여 있으면 새 이슈 타입 **`카테고리혼재`**로 분리하고, 하나로 통일된 그룹만 기존처럼 개수 비교. 이미 처리된 `20260630-0007933`은 되돌리지 않음(사용자 확인).
- **검증**: 실제 데이터엔 마침 완료일 비어있는 카테고리혼재 건이 없어서, 문제의 주문(브라5+팬티5)을 그대로 재현한 합성 데이터로 로직 검증 — `카테고리혼재`로 정확히 분리됨, 정상 케이스는 그대로 통과, 무관한 카테고리 주문은 결과에 안 나타남 확인.
- **변경 파일**: `cx-exchange-dashboard/api/reconcile.js`, `cx-exchange-dashboard/server/index.js`, `cx-exchange-dashboard/src/pages/Reconcile.tsx`(이슈 배지 색상 추가), `인수인계.md`, `cx-exchange-dashboard/탭별_기능가이드.md`

## 2026-07-09

### 반품입고시트 열 구조 변경 사고 대응 — 모든 열 참조를 헤더 텍스트 기준으로 전환
- **사고 발견**: 사용자가 주문번호 `20260629-0018516`이 반품-교환 연동에서 "확인필요"로도, "정상처리"로도 안 보이는 이유를 물어봄 → 조사 중 반품입고시트(`판토스_입고리스트`)에서 **"수동구분" 열이 삭제**되어 그 뒤 모든 열이 한 칸씩 밀린 걸 발견. `reconcile.js`·`recompute-category.js`·`archive-completed.js`(api/server 양쪽 총 6개 파일)가 전부 열을 고정 문자/숫자(J열, T열, `ORDER_NO_IDX=9` 등)로 하드코딩하고 있어서, 주문번호 대신 고객 이름을, 실수량 대신 "불량확인" 값을 조용히 읽고 있었음.
  - 다행히 감사 결과 E열(구분값) 자체는 거의 손상 안 됨(1132건 중 1130건 일치) — "재계산→반영"이 버그 상태에서 실행된 적 없음. 데이터 복구는 불필요, 코드만 수정.
- **해결**: 모든 열 참조를 **헤더 텍스트 기준으로 매번 찾도록** 전환. `api/_columns.js` 신규 — `resolveColumns(headerRow, spec)`가 헤더에서 이름으로 위치를 찾고, 못 찾으면 조용히 넘어가지 않고 **에러를 던짐**. `indexToLetter()`도 함께 추가.
  - `api/reconcile.js` + `server/index.js`: 반품입고시트(`완료일`,`구분`,`주문번호`,`상품명/옵션명`,`수량`,`실수량`)·교환접수시트(`출고일`,`지불방법`,`주문번호`,`교환 전 옵션`,`상품명`,`교환 출고 옵션`,`수량`) 전부 헤더로 위치 확인. 읎기 시작점을 C열/B열 등 제각각이던 걸 **항상 A열부터**로 통일해 오프셋 계산 실수 여지 제거. (겸사겸사 `server/index.js`엔 없던 선교환 스킵 로직도 `api/reconcile.js`와 동일하게 추가)
  - `api/recompute-category.js` + `server/scripts/recompute-category.mjs`: 반품입고시트 `입고코드번호`(예전 T열, 지금 S열), `구분`(E열) 열을 헤더로 찾음. 교환접수시트 3개의 `[자동]` 식별자 열은 시트마다 헤더 이름이 다름(자사몰='[자동]', 외부몰='[자동]입고코드번호', **불량='헤더 이름 자체가 없음, 공백'**) → prefix 매칭 추가하고, 불량 시트는 이름으로 못 찾으면 고정 위치(P열)로 대체 + 경고 메시지 응답에 포함.
  - `api/archive-completed.js` + `server/scripts/archive-completed.mjs`: `주문번호`/`완료일` 열을 헤더로 찾음. **안전장치 추가**: 히스토리 이관 직전 반품입고시트와 히스토리시트 헤더가 완전히 같은지 검증하고 다르면 에러로 중단(이 로직은 행을 배열로 그대로 복사해서 열 순서가 같다는 가정이 필수라, 검증 없이는 오늘 같은 사고가 또 날 수 있음).
- **검증**: 문제의 원인이었던 행(385~389행, 주문번호 `20260629-0018516`)을 재조회해 이제 정확히 읎히는지 확인. `recompute-category`/`archive-completed`/`reconcile`(자사몰+외부몰) 4개 핸들러를 실제로 호출해 200 응답과 정상 분포 확인. 자사몰/외부몰 둘 다 현재 완료일 비어있는 대기 건이 실제로 0건임을 별도 확인해 "빈 결과"가 버그가 아님을 검증.
- **변경 파일**: `cx-exchange-dashboard/api/_columns.js`(신규), `cx-exchange-dashboard/api/reconcile.js`, `cx-exchange-dashboard/api/recompute-category.js`, `cx-exchange-dashboard/api/archive-completed.js`, `cx-exchange-dashboard/server/index.js`, `cx-exchange-dashboard/server/scripts/recompute-category.mjs`, `cx-exchange-dashboard/server/scripts/archive-completed.mjs`

## 2026-07-08 (3)

### 상품별 상세 분석 페이지 실제 구현 (목업 → 실데이터)
- **배경**: `/product-detail`이 완전 정적 목업(2024년 날짜, 가짜 상품명 하드코딩)이었던 걸 실데이터로 전면 재작성. 판매량 데이터가 없어 교환율/불량률(%), AI 질의응답, 운영 로그는 이번 범위에서 제외.
- **구현**: 상품명 검색(자동완성) → 선택 시 해당 상품만 필터링한 딥다이브 표시. 기존 `lib/exchange.ts` 헬퍼(`normalizeChannel`, `swapDirection`, `classifyDefect`, `leadTimeDays`, `median`)를 그대로 재사용해 채널별 비중, 사이즈업/다운 방향, 옵션 교환 흐름 TOP5, 불량 유형 분포, 주간 리드타임 추이(최근 8주)를 구현.
- **재고 부족 알림 버그 수정 덕에 추가된 섹션**: 오늘 고친 재고관리 시트 J열 인식 문제 덕분에, 원래 "판매량 데이터 없어 불가능"으로 분류했던 **옵션별 현재 재고** 테이블도 이번에 같이 넣음 (COLOR/SIZE별 재고 + 100개 미만 LOW 배지).
- **검증**: 실제 상품("쿨핏 브라 볼륨핏")과 불량 이력이 없는 상품("뉴베이직 코튼 와이드 팬츠") 둘 다 테스트 — 빈 상태 문구 정상 표시, 콘솔 에러 없음, 전 섹션 스크린샷으로 확인.
- **변경 파일**: `cx-exchange-dashboard/src/pages/ProductDetail.tsx` (전면 재작성)

## 2026-07-08 (2)

### 재고 부족 알림 페이지 버그 수정 — 재고관리 시트 J열 인식 안 되던 문제
- **문제**: `/low-stock-alerts` 화면이 항상 빈 화면이었음. 원인은 재고관리 시트의 재고수량이 **J열**에 있는데, J열은 헤더행(1행)에 이름이 없어서 `google-spreadsheet` 라이브러리(`row.get(header)` 방식)가 그 컬럼 자체를 인식하지 못했기 때문. 기존 코드는 존재하지도 않는 `row.INVENTORY` 키를 찾고 있었음. J열은 다른 시트에서 수식으로 연동해 채우는 열이라 헤더를 새로 넣지 않기로 결정(사용자 확인) → 코드에서 직접 처리.
- **해결**: `api/dashboard-data.js`, `server/index.js` 양쪽에 `'재고관리'!J2:J` 범위를 raw REST로 별도 조회해 `row.__rowIndex` 기준으로 매칭, `row['재고']` 필드로 붙여주는 로직 추가. `api/_sheets.js`에 `getJwt()`를 export해 재사용(중복 방지). `src/pages/LowStockAlerts.tsx`는 `row.INVENTORY` → `row['재고']`로 수정.
- **검증**: 시트 여러 지점(100/500/1000/2000/3000/4000행)을 직접 대조해 100% 일치 확인. 전체 5,041개 품목 중 실제 데이터가 있는 4,847개 중 4,825개가 재고 100개 미만(SKU×컬러×사이즈 세분화가 심해서 자연스러운 분포). 로컬 서버로 화면 렌더링까지 확인.
- **참고**: 이 사실은 메모리(`inventory-sheet-stock-column.md`)에 기록해둠 — 재고 관련 작업 시 항상 참고.
- **변경 파일**: `cx-exchange-dashboard/api/dashboard-data.js`, `cx-exchange-dashboard/api/_sheets.js`, `cx-exchange-dashboard/server/index.js`, `cx-exchange-dashboard/src/pages/LowStockAlerts.tsx`

## 2026-07-08

### 반품-교환 연동에 외부몰 추가 + Vercel 배포 장애 대응
- **반품-교환 연동(`/reconcile`) 외부몰 확장**: 지금까지 자사몰 교환만 처리하던 자동화를 외부몰까지 확장. `[자사몰]/[외부몰] 교환` 시트가 B~K열 순서가 동일하고, 반품입고시트 구분값(자사몰교환/외부몰교환/불량교환)이 이미 있어 기존 매칭 로직을 그대로 재사용. 완료 히스토리 기준 외부몰교환 1,559건으로 자사몰(8,907건) 다음으로 큰 볼륨이라 확장 가치 확인 후 진행 (불량은 이번 범위 제외 — 불량 사유 필드가 지불방법과 무관해 별도 설계 필요).
  - 외부몰 지불방법 값이 자사몰과 달라(`결제`/`동봉확인`/`동봉요청` 등) 카테고리별 `validPay` 세트를 분리(`CATEGORY_CONFIGS`).
  - `api/reconcile.js`에 `category` 파라미터 추가(`자사몰교환`/`외부몰교환`, 기본값 자사몰로 하위호환).
  - **로컬 개발서버(`server/index.js`)에 동일 로직이 중복 구현돼 있던 것을 발견** — 프로덕션(Vercel)은 `api/reconcile.js`를 쓰지만 로컬은 별도 카피를 쓰고 있어서, 로컬 테스트가 되게 하려면 양쪽 다 고쳐야 했음. 두 파일 모두 카테고리 파라미터 반영.
  - `src/pages/Reconcile.tsx`에 자사몰/외부몰 탭 UI 추가, 탭 전환 시 결과 초기화.
  - 검증: 실제 반품입고시트 조회 결과 현재 자사몰/외부몰 둘 다 미처리 대상 0건(자사몰 76건은 이미 확인완료일 채워짐, 확인 완료)이라 실데이터 종단 테스트는 다음 반품 발생 시 필요. 로직은 코드 트레이스로 검증.
- **Vercel 배포 장애 발견 및 해결**: 어제(2026-07-07) 저장소를 비공개로 전환한 뒤 커밋 4개가 전부 "Deployment was blocked"로 실패해 사이트가 예전 버전에 멈춰 있었음. 원인은 두 가지가 섞여 있었음:
  1. Vercel 프로젝트의 "Vercel Authentication"(Deployment Protection)이 켜져 있어서 팀 멤버가 아니면 배포 URL 자체에 접근 불가 → 껐음.
  2. 더 근본적 원인: **Hobby(무료) 플랜은 비공개 저장소에서 프로젝트 협업자가 아닌 계정(`part-cx`)의 커밋으로는 배포를 차단함**. 저장소를 공개로 되돌려서 해결. 시크릿 유출은 없었음(`service-account.json`은 애초에 `.gitignore` 처리돼 커밋된 적 없음, git 히스토리 전수 확인).
  - 교훈: 저장소 공개/비공개 전환처럼 외부 서비스(Vercel 등) 연동에 영향을 줄 수 있는 설정은 변경 전에 영향 범위를 먼저 확인해야 함.
- **변경 파일**: `cx-exchange-dashboard/api/reconcile.js`, `cx-exchange-dashboard/server/index.js`, `cx-exchange-dashboard/src/pages/Reconcile.tsx`

## 2026-07-07

### 종합 대시보드 경영보고 톤으로 재기획 — 그래프 개선 + 회수지연/비용/부서별 요약 추가
- **배경**: 종합 대시보드가 각 채널 페이지의 축소판에 머물러 있다는 피드백("쓸데없는 게 많다") + 추이 그래프가 계단식(stepAfter)이라 뾰족뾰족 보인다는 피드백. 역할을 "10초 안에 전체 상황 파악 + 트리아지 + 부서별 핵심 수치"로 재정의하고 톤을 **경영 보고 중심**으로 정함.
- **그래프 수정**: 데이터 없는 날짜(주말 등)가 통째로 빠지며 x축이 불규칙해지던 문제 해결 — 기간 내 모든 날짜를 0으로 채우고, 계단형 라인 → 그라데이션 영역차트(monotone)로 교체.
- **불필요 지표 제거**: 선교환/무료교환 비중 카드를 종합에서 제거(자사몰 페이지에 이미 있어 중복). 남은 리드타임/처리율 2개는 4칸→2칸 그리드로 정리.
- **신규 인사이트 발굴** (실데이터 재검토, 기존에 안 쓰던 시트 열 활용):
  - 회수내역 열의 `미집화` 상태 + 접수일 7일+ 경과 → **회수 지연** 788건 (자사몰+불량 시트 기준; 외부몰엔 회수내역 필드 없어 제외)
  - 연락처 기준 기간 내 3회+ 교환 고객 → **반복교환 고객** 1,500명+ / 전체 5,344명(28%)
  - 택배비 열 합계 → **교환 배송비 수취 총액** (전 채널)
  - 불량 시트 상품별 현재기간 vs 직전기간 비교(5건+ 이면서 2배+ 급증) → **품질 급증 경보** — 평소엔 안 보이고 조건 충족 시에만 상단 배너 노출 (검증: 기간을 짧게 바꾸면 배너가 사라지는 것 확인)
  - `lib/exchange.ts`에 `needsRecovery`, `shippingFee` 헬퍼 추가 (자사몰 페이지의 택배비 파싱 중복 로직도 이걸로 통합)
- **레이아웃**: 새 섹션을 늘리지 않는 원칙으로, 리스크&비용 지표 스트립을 2→5개로 확장하고, 하단의 제네릭 문단형 "최근 리포트 요약" 카드를 **부서별 현황**(생산/QC·물류·재무 원라이너 + 해당 딥다이브 페이지 링크)으로 교체.
- **저장소 비공개 전환**: 별개로, 저장소(`khlee-lang/CX-`)가 퍼블릭 상태였던 것을 발견해 비공개로 전환. 시크릿 유출은 없었음(`service-account.json`은 애초에 `.gitignore` 처리돼 커밋된 적 없음 확인) — 스프레드시트 ID·사내 프로세스 노출 방지 목적.
- **변경 파일**: `src/lib/exchange.ts`, `src/pages/OverviewDashboard.tsx`, `src/pages/JasaMallExchange.tsx`(택배비 파싱 통합), `cx-exchange-dashboard/server/scripts/archive-completed.mjs`(기존 미추적 파일 커밋)

### 분석 대시보드 4개 페이지 고도화 (종합/자사몰/외부몰/불량) — 가짜 수치 제거 + 타부서 공유용 지표 추가
- **배경**: 종합/자사몰/외부몰/불량 분석 화면을 고도화하면서, CX 외 부서(생산/QC·MD·물류·재무)도 유의미한 데이터를 가져갈 수 있도록 지표를 재설계.
- **발견된 문제 (실데이터 검증)**:
  1. **하드코딩된 가짜 수치**: 자사몰 `mom: 12`, 외부몰 `mom: 15`, 종합 TOP상품의 `+4%`·"Fashion Apparel Item" — 전부 실제 계산이 아닌 고정값이었음 → 실계산으로 교체.
  2. **출고일 더티값으로 처리율/미처리 오집계**: 출고일 열에 날짜가 아닌 값(`입고0707`, `미입고`, `-`, 메모 등)이 900건+ 있는데, 기존 코드는 "값이 있으면 처리완료"로 간주 → 재고대기/미입고 건이 처리완료로 잡혔음.
  3. **불량 분류 이중집계 + 기타 블랙홀**: `올트임` 키워드가 봉제·원단 두 카테고리에 모두 걸려 이중집계. 실제 사유 상위권인 `확인필요`(294)·`변심`(268)·`전산오처리`(140)·`내품오류`(116)는 전부 "기타"로 뭉개져 있었음.
  4. **페이지마다 17MB API 전체 재요청**: 탭 이동 시마다 2.5만 행을 다시 받음.
- **변경 내용**:
  - `src/lib/exchange.ts` **신규** — 공용 유틸: 출고상태 판별(`shipStatus`: 출고완료/재고입고대기/반품미입고/미출고/보류), 리드타임 계산, 옵션 파싱(`[블랙-M]` → 색상+사이즈, 브라사이즈 `7580A` 지원), 사이즈 업/다운 방향 판정, 불량사유 단일분류(제품결함/운영·물류오류/비불량/확인필요 4그룹 × 8카테고리), 채널 정규화(`(불)선교환` → 자사몰).
  - `src/api/sheets.ts` — dashboard-data 5분 메모리 캐시 (새로고침 버튼은 force로 무시).
  - **종합 대시보드** — 미출고를 상태별(단순미출고/재고대기/미입고)로 분해, 운영지표 스트립 추가(출고 리드타임 중앙값·출고 처리율·선교환 비중·무료교환 비중), 추이 차트에 불량 라인 추가, TOP상품에 실제 비중%·불량건수 표시.
  - **자사몰 교환 분석** — 실제 전기 대비 증감률, 정책/비용 지표(무료교환·입금요청 비중, 선교환 비중, 교환 배송비 수취액 합계), **사이즈 교환 방향 분석** 신규(사이즈업/다운/색상만변경/동일옵션 + 사이즈업·다운 TOP5 상품 → 사이즈 가이드/그레이딩 개선 신호. 전체 데이터 기준 사이즈업 10,693 vs 다운 2,072로 "작게 나온다" 신호가 압도적).
  - **외부몰 교환 분석** — 실제 전기 대비 증감률(음수 표시 지원), **채널별 운영 현황 테이블** 신규(채널별 건수·비중·출고처리율·리드타임 중앙값·불량 접수), 파이차트 상위 3→5개.
  - **불량 교환 분석** — 전면 재작성. 그룹 KPI(전체/제품결함/운영·물류오류/비불량/확인필요)로 "진짜 품질 문제"를 분리, 월별 그룹 스택 추이, 사유별 상세(8카테고리 단일분류+드릴다운), 재고관리 시트 ITEM 매칭(99.5%)으로 **상품 대분류(브라/팬티 등)별 제품결함 분포**, 채널별 불량 접수, TOP3 상품은 제품결함 건만 집계(변심·전산오류 제외).
- **알아둘 데이터 이슈** (시트 정리 필요, 코드로는 방어 처리함): 접수일에 미래 날짜(`2026-07-08`) 존재, 출고일에 `2026-06-06-1` 같은 변형 날짜, 불량 사유 미입력 7건, `확인필요` 294건은 사유 보완 필요.
- **변경 파일**: `src/lib/exchange.ts`(신규), `src/api/sheets.ts`, `src/pages/OverviewDashboard.tsx`, `src/pages/JasaMallExchange.tsx`, `src/pages/OebuMallExchange.tsx`, `src/pages/DefectiveAnalysis.tsx`

## 2026-07-06

### 반품입고시트 근본 성능 문제 해결 — E열 거대 수식 → 코드 대체 + 완료건 히스토리 이관
- **문제**: `/reconcile` 미리보기가 300초까지 늘려도 타임아웃되거나 "The service is currently unavailable" 오류. 원인 추적 결과 코드 문제가 아니라 **구글시트 자체가 근본적으로 느린 것**으로 확인.
- **근본 원인**: 반품입고시트(`판토스_입고리스트`) E열("구분")에 `ARRAYFORMULA(VLOOKUP(REGEXREPLACE(...), LET(...74,539행짜리 헬퍼시트를 매번 재구성...)))` 형태의 거대 수식이 29,028행 전체에 걸려있었음. 이 헬퍼시트(`[자동]자동화_교환시트연결`)조차 `[자사몰]/[외부몰]/[불량] 교환` 3개 시트를 IMPORTRANGE로 실시간으로 끌어오는 또 다른 수식 체인이었음. 구글시트는 API로 값 하나만 읽어도 이 계산을 다 마쳐야 응답을 주기 때문에, 관련 없는 열을 읽어도 지연/실패 발생.
- **해결 (2단계)**:
  1. **완료 건 자동 분리 구조 도입**: `판토스_입고리스트`(미처리 전용, 완료일 비어있는 건만) / `판토스_입고_히스토리`(완료 건 보관, 신규 시트) 로 분리. 같은 주문번호 그룹은 **모든 행의 완료일에 날짜(YYYY-MM-DD 포함, 접두어 텍스트 허용)가 있어야만** 히스토리로 이관. 하나라도 없으면 그룹 전체 보류 (부분완료/오배송 방지). 주문번호가 "확인필요"처럼 실제 주문번호가 아닌 경우도 완료일 기준으로만 판단(별도 블랙리스트 없음).
     - 원본 시트 기준: 28,028행 → 미처리 661행 + 히스토리 27,367행으로 정리.
     - 실행 스크립트는 1회성이라 저장하지 않음(터미널에서 직접 실행). 매일 자동화는 **당분간 보류** — 구글시트 폼/양식이 바뀔 수 있어 수동 운영이 안전하다고 판단.
  2. **E열 수식 → 코드 대체**: `server/scripts/recompute-category.mjs` 신규. `[자사몰]/[외부몰]/[불량] 교환` 시트의 P열(식별자)을 직접 읽어 매칭 테이블을 만들고, 반품입고시트 T열(입고코드번호=주문번호+상품명옵션)과 대조해 구분값을 계산. 기존 수식 결과와 99.95%+ 일치 검증(불일치 전부 원본 데이터 자체가 이미 `#N/A`/`#REF!`였던 셀). E3의 ARRAYFORMULA 삭제 후 계산값을 정적으로 기입. **원할 때 수동 실행**(`node scripts/recompute-category.mjs`, `--dry-run` 옵션으로 미리보기 가능).
- **결과**: 반품입고시트 읽기 속도 185초(실패) → **400ms대**로 정상화.
- **`api/reconcile.js` 단순화**: 미처리함이 항상 작게 유지되므로, 기존의 "뒤에서부터 청크 단위로 마지막 데이터 행 찾기"(`findLastDataRow`, `getSheetRowCount`) 로직을 완전히 제거하고 전체 범위를 한 번에 읽도록 변경. 타임아웃/재시도 안전장치(2026-07-03 도입분)는 유지.
- **작업 중 사고와 교훈**: 사본에서 테스트하던 중, 중간 단계 실패를 확인 안 하고 다음 단계(히스토리 비우기)를 진행시켜 17,555행이 일시적으로 사라짐. **구글시트 버전 기록으로 복구 성공** (데이터 유실 없음). 이후 모든 스크립트에 "각 단계 성공 확인 후에만 다음 단계 진행" 원칙 적용. 또한 `deleteDimension`으로 행을 삭제하면 시트 크기 자체가 줄어들어, 그 자리에 다시 쓰려면 그리드 크기(`gridProperties.rowCount`)를 먼저 늘려야 한다는 것도 확인.
- **적용 전 안전장치**: 원본 적용 전 스프레드시트 전체 수동 백업(`파일 → 사본 만들기`) 확보 후 진행.
- **변경 파일**: `cx-exchange-dashboard/api/reconcile.js`, `cx-exchange-dashboard/src/pages/Reconcile.tsx`(안내 문구 수정), `cx-exchange-dashboard/server/scripts/recompute-category.mjs`(신규), 구글시트(`판토스_입고리스트`/`판토스_입고_히스토리`/E열).

## 2026-07-03

### 반품-교환 연동(reconcile) 타임아웃/오류 해결 + 선교환 처리 추가
- **문제**: "미리보기 실행" 버튼이 `Vercel Runtime Timeout Error: Task timed out after 60 seconds`로 자주 실패. maxDuration을 늘렸더니 이번엔 구글 시트 API가 `The service is currently unavailable`(503류 일시 오류)를 반환.
- **원인**: 마지막 데이터 행을 알아내려고 반품입고시트 J열/교환접수시트 G열 **전체(수만 행)를 한 번에 읽는 요청**이 병목이자 구글 API 거부의 원인이었음.
- **조치 (3단계, 순서대로 적용)**:
  1. `vercel.json` — `api/reconcile.js` `maxDuration` 60 → 300초
  2. `api/reconcile.js` — 구글 API가 일시적 오류(503 등)를 주면 최대 3회 지수 백오프로 자동 재시도 (`withRetry` 헬퍼, `sheetsGet`/`sheetsBatchUpdate`에 적용)
  3. `api/reconcile.js` — 열 전체를 한 번에 읽던 방식을 제거. 시트 크기 상한은 메타데이터(`gridProperties.rowCount`)로 가볍게 조회하고, **뒤에서부터 1만 행씩 나눠서(`findLastDataRow`)** 데이터가 있는 마지막 행을 탐색하도록 변경. 이후 최근 7,000행만 읽는 기존 로직은 유지.
- **결과**: 미리보기 실행 정상 동작 확인 (62건 정상 처리 / 9건 이슈, 타임아웃·오류 없음).
- **선교환 건 처리 추가**: 반품이 들어오기 전에 이미 새 상품을 출고한 "선교환" 건(교환접수시트 출고일 열에 이미 날짜가 채워진 경우)을 감지해, **출고일은 건드리지 않고 반품입고시트 확인완료일만 기입**하도록 로직 추가.
  - 미리보기 화면 출고일 컬럼에 `이미출고됨(2026-07-03)` 형태로 표시, 사유 컬럼에 `2026-07-03 선출고 → 확인완료일만 기입` 표시
  - `parseExchanges`에 `existingShipDate` 필드 추가, `reconcile`에서 우선 분기 처리, `applyActions`에서 `skipShipWrite` 플래그로 해당 건은 교환접수시트 쓰기 제외
- **변경 파일**: `cx-exchange-dashboard/vercel.json`, `cx-exchange-dashboard/api/reconcile.js`

## 2026-06-30

### 자동화 탭 신설 — 반품입고-교환 연동 (reconciler)
- **무엇**: 왼쪽 사이드바에 "자동화" 섹션 추가. 첫 번째 항목으로 **반품-교환 연동** 페이지 신설. `reconciler.py` 로직을 JS로 이식해 버튼 한 번으로 실행 가능.
- **흐름**: ① 미리보기 실행 → 정상 N건 / 이슈 M건 테이블 확인 → ② 시트에 반영 (확인 후 실제 시트 업데이트)
- **읽는 시트**: 반품입고시트 `판토스_입고리스트`!C:O + 교환접수시트 `[자사몰] 교환`!B:K (최근 7,000행만, 타임아웃 방지)
- **변경 파일**:
  - `api/reconcile.js` 신규 — 반품입고시트 + 교환접수시트 매칭 → 출고일/확인완료일 기록. Vercel 서버리스 함수 (maxDuration 60초)
  - `src/pages/Reconcile.tsx` 신규 — 미리보기/반영 2단계 UI, 정상/이슈 테이블 표시
  - `src/App.tsx` — `/reconcile` 라우트 추가
  - `src/components/layout/Sidebar.tsx` — "자동화" 섹션 + "반품-교환 연동" 메뉴 추가
  - `server/index.js` — 로컬 개발용 `/api/reconcile` 라우트 추가
  - `vercel.json` — reconcile 함수 maxDuration 60초, SPA rewrite 규칙 추가
  - `.gitignore` — `vercel.json` 예외 추가
- **버그픽스 (같은 날)**:
  - SPA 라우팅 404 → `vercel.json`에 rewrite 규칙 추가
  - 시트 이름 미지정 → 첫 번째 탭(`[CX 휴가일정]`)에 잘못 읽고 쓰던 문제 → 모든 읽기/쓰기에 시트 이름 명시
  - 타임아웃(22k행 전체 읽기) → 단일 컬럼으로 행 수 파악 후 최근 7,000행만 읽도록 최적화

### Vercel 단일 배포 전환 — Python → JavaScript 알림톡 엔진 이식
- **무엇**: 백엔드를 Railway 없이 Vercel 하나로 운영할 수 있도록 전환. Python 알림톡 로직(alimtalk_core.py + send_single_python.py)을 JavaScript로 완전히 이식해 Node 서버 없이 Vercel 서버리스 함수로 직접 처리.
- **왜**: Railway 유료, Vercel은 프론트+백엔드 같은 도메인에서 무료로 운영 가능. Python subprocess 호출 구조가 Vercel과 맞지 않아 JS로 전환.
- **구조 변화**: `브라우저 → Express(3001) → Python exec` → `브라우저 → Vercel /api/* 서버리스 함수(JS)`
- **변경 파일**:
  - `api/_alimtalk.js` 신규 — 루나소프트 알림톡 템플릿 50195~50201 + 입고/옵션/미입금 분기 로직 전체
  - `api/_sheets.js` 신규 — 구글시트 JWT 클라이언트 공통 모듈
  - `api/dashboard-data.js` 신규 — GET 서버리스 함수
  - `api/update-cell.js` 신규 — PATCH 서버리스 함수
  - `api/send-alimtalk.js` 신규 — POST 서버리스 함수
  - `vite.config.ts` — 로컬 개발용 `/api` → `localhost:3001` proxy 추가
  - `src/api/sheets.ts` — API_BASE_URL을 `/api`로 변경 (Vercel 동일 도메인)
  - `package.json` — google-spreadsheet, google-auth-library 추가
- **로컬 개발**: 기존 `server/index.js` 그대로 유지 (fallback). Vite proxy 덕분에 로컬에서도 `/api` 경로로 동작.
- **배포 시 필요한 Vercel 환경변수**: `GOOGLE_SERVICE_ACCOUNT_JSON` (service-account.json 내용 전체)

### GitHub 연결 및 배포 준비 (Vercel + Railway)
- **무엇**: git 초기화 + GitHub(`khlee-lang/CX-`) 연결 및 초기 push. Vercel(프론트)/Railway(백엔드) 배포를 위한 코드 수정.
- **왜**: 서버 배포를 위해 버전 관리 시작. 민감 파일(service-account.json, verish-part-*.json)은 .gitignore로 제외.
- **변경 파일**:
  - `src/api/sheets.ts` — API URL을 `VITE_API_BASE_URL` 환경변수로 분리 (로컬 fallback 유지)
  - `server/index.js` — 구글 인증키를 `GOOGLE_SERVICE_ACCOUNT_JSON` 환경변수로 처리 / Python·스크립트 경로 환경변수화 / CORS origin 환경변수(`ALLOWED_ORIGINS`)로 처리
  - `server/nixpacks.toml` 신규 — Railway에서 Node 22 + Python3 + requests 설치
  - `cx-exchange-dashboard/vercel.json` 신규 — Vercel 빌드 설정
  - `.env.example` 2개 신규 — 설정해야 할 환경변수 안내용
  - 빌드 에러 수정: `DefectiveAnalysis.tsx` 미사용 함수 삭제, `OebuMallExchange.tsx` 미사용 변수 `_e`로 변경

## 2026-06-12

### 인수인계.md 작성
- **무엇**: 클로드 계정 변경(같은 맥, 같은 macOS 사용자)에 대비해 폴더 루트에 `인수인계.md` 신규 작성. 빠른 시작/처리 흐름/파일별 역할표/사전조건/결과 읽는 법/트러블슈팅/키 위치/첫날 체크리스트 + "문서와 실제가 달랐던 점" 부록 포함.
- **왜**: 새 계정·다른 사람이 폴더만 열어도 추가 설치 없이 이어서 운영할 수 있도록.
- **어디**: `인수인계.md` (신규)
- **조사로 검증한 핵심 사실**: ① crontab에 이 폴더 자동실행 항목 없음(전부 수동) ② `교환미처리건알림톡.py`는 존재하지 않는 함수 호출하는 레거시(작동 안 함), 실제 알림톡은 `send_single_python.py`+`alimtalk_core.py` 경로 ③ node v22.14.0·node_modules·`/usr/bin/python3 requests` 모두 설치되어 추가설치 불필요 ④ 하드코딩 절대경로: `server/index.js:155-156`, `교환미처리건알림톡.py:15` ⑤ 구글키 3곳 동일계정(cx-part@verish-part), 루나소프트 API키는 `index.js:115-118`·`alimtalk_core.py` ⑥ `대시보드_실행방법.md` 마지막 줄("배경에서 실행해둠")은 사실과 다름.

### 교환 미처리 관리 — 입고/미입고 필터 개편
- **무엇**: 사유 유형 필터에서 "전체"가 입고된 건을 숨기던 동작을 바꿔, **전체는 입고/미입고 모두 표시**하도록 변경. **`📭 입고안됨` 필터를 신규 추가**(하늘색)하고, 기존 `📦 입고됨`과 함께 입고 여부를 독립된 배타 축으로 분리.
- **왜**: 라벨이 "전체"인데 실제로는 미입고 건만 보여서, 출고일이 `입고MMDD`로 채워진 건(예: 주문번호 `20260518-0000128`)이 기본 화면에서 누락되어 "왜 조회가 안 되냐"는 혼란이 있었음. 입고된 건과 안 된 건을 명확히 구분해서 보려는 목적.
- **어디**: `cx-exchange-dashboard/src/pages/StuckCases.tsx`
  - `REASON_FILTERS`에 `입고안됨` 추가
  - `reasonCounts`에 `입고안됨` 카운트 추가
  - `stuckCases` 필터 로직: 입고됨/입고안됨을 배타 토글로, 둘 다(또는 둘 다 미선택)면 입고 필터 미적용. "전체"(필터 없음)는 전부 노출
  - 필터 버튼 스타일에 `입고안됨`(sky 색상) 추가
- **참고/후속**: "전체"가 입고된 건까지 포함하게 되어, 필터 없이 "조건 전체 발송"을 누르면 입고된 건에도 알림톡이 나갈 수 있음 → 일괄 발송 시 `📭 입고안됨`을 먼저 켜는 흐름 권장. (이 부분 동작 추가 조정은 미적용)

---

## 과거 이력 (파일 날짜로 재구성 — 정확하지 않을 수 있음)

> ⚠️ 이 프로젝트는 git 저장소가 아니라 커밋 히스토리가 없습니다. 아래는 파일 생성/수정 날짜로 추정한 대략적 타임라인이며, 세부 내용은 부정확할 수 있습니다. 이 날짜 이후부터는 위쪽에 정확히 기록합니다.

- **2026-03-27** — 프로젝트 초기 생성. Vite + React 19 + TypeScript + Tailwind 골격. 초기 페이지: 자사몰 교환(`JasaMallExchange`), 외부몰 교환(`OebuMallExchange`), 리드타임(`LeadTime`), 개요(`OverviewDashboard`), 상품 상세(`ProductDetail`), 리포트 센터(`ReportCenter`). 구글 서비스 계정 키(`verish-part-*.json`) 추가.
- **2026-03-31** — 불량 분석 페이지(`DefectiveAnalysis`) 추가.
- **2026-04-08** — 교환 미처리건 알림톡 파이썬 스크립트(`교환미처리건알림톡.py`) 추가.
- **2026-04-09** — 단건 발송 파이썬 스크립트(`send_single_python.py`) 추가.
- **2026-04-10** — 백엔드 서버(`server/index.js`) + 구글시트 API 연동(`src/api/sheets.ts`) 구축. 재고 부족 알림 페이지(`LowStockAlerts`) 추가.
- **2026-04-16** — 교환 실적 페이지(`ExchangePerformance`) 추가.
- **2026-04-24** — 대시보드 실행 방법 문서(`대시보드_실행방법.md`) 작성.
- **2026-05-06** — 알림톡 코어 모듈(`alimtalk_core.py`) 정비.
